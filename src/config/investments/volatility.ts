// The smaller the number, the more it changes
// EG: sop has a price of 25, so can change by a max of 25 / the volatility set

class volatility {
  static sop = 7.5;
  static rbx = 7.5;
  static apl = 7;
}

export default volatility;
